这是一个仿照QCA扫描状态机实现的简易扫描状态机。
QCA的实现比较复杂，比如支持使用软中断/tasklet来触发事件，扫描过程的事件类型也比较多。
出于学习的目的，进行了简化（实际上，在我所知的机型上面，其实也没有用到软中断或者tasklet，都用的定时器）

从这个模块编写可以学到：
[0] 较为基础的状态机编程
[1] Linux驱动（多个源文件） 和 的Makefile 的编写
[2] Linux内核定时器的使用